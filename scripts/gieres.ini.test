# Gieres "Radio Radio" endpoint override for bootamp.
# Copy to ~/.config/bootamp/gieres.ini and edit. Keys:
#   base_url — archive server base (LAN default, public fallback below).
# base_url = http://192.168.1.154:13080
# base_url = https://gieres.cytr.us